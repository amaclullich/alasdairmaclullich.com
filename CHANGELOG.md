# Changelog

Notable public-site changes are recorded here. Dates use the `YYYY-MM-DD` format.

## Unreleased

- Raise the smallest interface text to a 14-pixel minimum at the default browser size.
- Place the analytics notice in normal document flow so that it cannot cover page actions.
- Document the policy for disclosed, independently reviewed Wikipedia and Wikidata requests.
